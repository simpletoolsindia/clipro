# GraalVM Native Image Configuration for CLIPRO
# See https://www.graalvm.org/latest/reference-manual/native-image/dynamic-configuration/

# Build-time options
--initialize-at-build-time=ch.qos.logback
--initialize-at-build-time=org.slf4j
--static-build

# Memory settings
# -J-Xmx=4G
# -J-Xms=2G

# Image building options
--no-fallback
--verbose
--report-unsupported-elements-at-runtime

# Java build options
# JavaAot阙.GenerateKind=native-inspect

# Report unreachable handlers at image build time
# RuntimeBuiltins阙.ReportUselessTypeHierarchies=true

# Agent-based configuration (used during tracing)
# Agent阙.ExpandProperties=true
# Agent阙.InstrumentationPreferences.ExpensiveGuardedTemplates=INCLUDE